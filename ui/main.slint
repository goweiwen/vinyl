import { Player, MusicService } from "views/player.slint";
import { Library, LibraryModel } from "views/library.slint";
import { Song, NowPlaying } from "model.slint";
import { Format } from "util.slint";
import "fonts/Nunito.ttf";

export { NowPlaying, MusicService, LibraryModel, Format }

export component MainWindow inherits Window {
    default-font-family: "Nunito";
    width: 640px;
    height: 480px;
    background: black;

    if NowPlaying.song.path != "": Player {
        autoplay: false;
    }
    
    if NowPlaying.song.path == "": Library {
    }
}
