export struct Song {
    path: string,
    title: string,
    artist: string,
    album: string,
    cover-art: image,
}

export struct NowPlaying {
    song: Song,
    progress: float,
    duration: float,
    is-playing: bool,
}

export struct Model {
    now-playing: NowPlaying,
    songs: [Song],
}
